const TaskListItem = require('../lib/TaskListItem.js');

describe('TaskListItem', () => {
    test('should create a new shape', () => {
        const taskListItem = new TaskListItem();
        expect(taskListItem).toEqual(expect.any(Object));
    }
    );
});

test('should create a new color', () => {
    const taskListItem = new TaskListItem();
    expect(taskListItem).toEqual(expect.any(Object));
});

test('should create a new border color', () => {
    const taskListItem = new TaskListItem();
    expect(taskListItem).toEqual(expect.any(Object));
});

test('should create a new border width', () => {
    const taskListItem = new TaskListItem();
    expect(taskListItem).toEqual(expect.any(Object));
});

test('should create a new border radius', () => {
    const taskListItem = new TaskListItem();
    expect(taskListItem).toEqual(expect.any(Object));
});

test('should create a new padding', () => {
    const taskListItem = new TaskListItem();
    expect(taskListItem).toEqual(expect.any(Object));
});

test('should create a new margin', () => {
    const taskListItem = new TaskListItem();
    expect(taskListItem).toEqual(expect.any(Object));
});

test('should create a new text color', () => {
    const taskListItem = new TaskListItem();
    expect(taskListItem).toEqual(expect.any(Object));
});

test('should create a new text size', () => {
    const taskListItem = new TaskListItem();
    expect(taskListItem).toEqual(expect.any(Object));
});

test('should create a new text', () => {
    const taskListItem = new TaskListItem();
    expect(taskListItem).toEqual(expect.any(Object));
});